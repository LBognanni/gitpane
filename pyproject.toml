[project]
name = "gitpane"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "unidiff>=1.0.0",
]

[dependency-groups]
dev = [
    "mypy",
    "pytest",
    "ruff",
]

[tool.ruff]
target-version = "py311"

[tool.mypy]
python_version = "3.11"
strict = true
